export interface IDropMotionItem {
	children: string | JSX.Element | JSX.Element[];
	customStyles?: string;
	translate: string;
}

export interface INavbarLink {
	href: string;
	name: string;
}
